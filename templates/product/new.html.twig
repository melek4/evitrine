{% extends 'base.html.twig' %}

{% block body %}
	{{form_start(formpro)}}
	{{form_widget(formpro)}}
	<button type="submit" class="btn btn-primary">Enregistrer</button>
	{{form_end(formpro)}}
{% endblock %}
